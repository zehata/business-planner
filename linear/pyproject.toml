[project]
name = "linear"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["business-planner-plugin"]

[tool.uv.sources]
business-planner-plugin = { path = "../../../business-planner-plugin/", editable = true }
